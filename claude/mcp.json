{
  "$comment": "Project MCP servers for Claude Code — symlinked to <target>/.mcp.json by install.sh. Mirrors the high-value Codex MCP surface. GitHub work uses the `gh` CLI (no MCP/token needed).",
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp"]
    }
  }
}
